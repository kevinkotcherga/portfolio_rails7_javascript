<!DOCTYPE html>
<html>
  <head>
    <title>Kevin Kotcherga - Développeur Web - Ruby On Rails - React - Full stack</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta property="og:image:type" content="/app/assets/images/login-page.png" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path('favicon.ico') %>

  </head>

  <body id="<%= controller.controller_name %>-<%= controller.action_name %>">
    <section >
      <div class="container">

        <%= render 'shared/header' %>
        <%= render 'shared/navbar' %>
        <%= render 'shared/switch_dark_mode' %>

        <%= yield %>
    </section>
  </body>
</html>
